@AGENTS.md

# Claude Code-specific guidance

- `AGENTS.md` and the referenced project documents are authoritative.
- Auto memory is never authoritative for architecture, sprint scope, or product
  decisions.
- Do not rewrite `AGENTS.md` or project documentation from auto memory.
- Use `/memory` when asked to confirm which instruction files were loaded.
- Do not run `/init` in a way that replaces existing project guidance.